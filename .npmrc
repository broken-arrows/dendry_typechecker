; vitest declares an optional peer on @types/node >=20, but this extension
; intentionally pins @types/node@^16 to match its `engines.vscode` floor. The
; peer is optional and only affects vitest's own typings, so we relax strict
; peer resolution rather than widen the extension's declared Node surface.
legacy-peer-deps=true
